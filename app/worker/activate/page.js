import WorkerActivationClient from "../../../components/WorkerActivationClient";

export const metadata = { title: "作業員アカウント作成 | SPEED ETC" };

export default function WorkerActivationPage() { return <WorkerActivationClient />; }
